#!/usr/bin/python3
ans=[]
def search_replace(my_list, search, replace):
    for i in range(0, len(my_list)):
        if my_list[i] == search:
            ans[i] = replace
        else:
            ans[i] = my_list[i]
    return ans
